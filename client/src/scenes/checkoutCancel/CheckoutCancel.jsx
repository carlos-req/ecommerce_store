const CheckoutCancel = () => {
    return (
        <div>
            Make me into a page that shows the checkout cancel and returns me
            back to shopping
        </div>
    );
};
export default CheckoutCancel;
